{% extends 'base.html.twig' %}

{% block title %}Hello AdminController!{% endblock %}

{% block content %}
    <h1>admin page</h1>
    <hr>
    <div>
        <a href="{{ path('quiz_page') }}">Existing quizzes</a>
    </div>
    <div>
        <a href="{{ path('question_page') }}">Existing questions</a>
    </div>
    <div>
        <a href="{{ path('users_table') }}">Existing users</a>
    </div>
{% endblock %}
